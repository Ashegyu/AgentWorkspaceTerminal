<Window x:Class="AgentWorkspace.App.Wpf.Agent.AgentInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ask Agent" Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Prompt" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox x:Name="PromptBox" Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap"
                 MinHeight="80" MaxHeight="200" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12"/>
        <TextBlock Grid.Row="2" Text="Working directory (optional)" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox x:Name="WorkDirBox" Grid.Row="3" Margin="0,0,0,16"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True" Width="75" Margin="0,0,8,0" Click="OnCancel"/>
            <Button Content="Ask" IsDefault="True" Width="75" Click="OnAsk"/>
        </StackPanel>
    </Grid>
</Window>
