<Window x:Class="AgentWorkspace.App.Wpf.PaneMessage.SendToPaneDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="패널로 텍스트 전송"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0b0e14"
        Foreground="#cbd2dc">
    <Window.Resources>
        <!-- Shared brushes matching MainWindow dark theme -->
        <SolidColorBrush x:Key="BgBrush"     Color="#0b0e14"/>
        <SolidColorBrush x:Key="ChromeBrush" Color="#11141b"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#1f2530"/>
        <SolidColorBrush x:Key="TextBrush"   Color="#cbd2dc"/>
        <SolidColorBrush x:Key="MutedBrush"  Color="#6b7280"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#79c0ff"/>
        <SolidColorBrush x:Key="InputBrush"  Color="#161b27"/>

        <!-- Dark ComboBox style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background"           Value="#161b27"/>
            <Setter Property="Foreground"           Value="#cbd2dc"/>
            <Setter Property="BorderBrush"          Value="#1f2530"/>
            <Setter Property="BorderThickness"      Value="1"/>
            <Setter Property="Padding"              Value="6,4"/>
            <Setter Property="Height"               Value="28"/>
            <Setter Property="FontFamily"           Value="Cascadia Code, Consolas"/>
            <Setter Property="FontSize"             Value="12"/>
        </Style>

        <!-- Dark TextBox style -->
        <Style TargetType="TextBox">
            <Setter Property="Background"           Value="#161b27"/>
            <Setter Property="Foreground"           Value="#cbd2dc"/>
            <Setter Property="BorderBrush"          Value="#1f2530"/>
            <Setter Property="BorderThickness"      Value="1"/>
            <Setter Property="Padding"              Value="6,4"/>
            <Setter Property="FontFamily"           Value="Cascadia Code, Consolas"/>
            <Setter Property="FontSize"             Value="12"/>
            <Setter Property="CaretBrush"           Value="#79c0ff"/>
        </Style>

        <!-- Dark CheckBox style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground"  Value="#6b7280"/>
            <Setter Property="FontFamily"  Value="Cascadia Code, Consolas"/>
            <Setter Property="FontSize"    Value="11"/>
        </Style>

        <!-- Button base style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="FontFamily"       Value="Cascadia Code, Consolas"/>
            <Setter Property="FontSize"         Value="12"/>
            <Setter Property="Padding"          Value="16,6"/>
            <Setter Property="Cursor"           Value="Hand"/>
            <Setter Property="BorderThickness"  Value="1"/>
        </Style>

        <!-- Primary (accent) button -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background"   Value="#1a3a5c"/>
            <Setter Property="Foreground"   Value="#79c0ff"/>
            <Setter Property="BorderBrush"  Value="#79c0ff"/>
        </Style>

        <!-- Secondary button -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background"   Value="#11141b"/>
            <Setter Property="Foreground"   Value="#6b7280"/>
            <Setter Property="BorderBrush"  Value="#1f2530"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Target pane label -->
        <TextBlock Grid.Row="0"
                   Text="대상 패널"
                   Foreground="{StaticResource MutedBrush}"
                   FontFamily="Cascadia Code, Consolas"
                   FontSize="11"/>

        <!-- Pane selector -->
        <ComboBox Grid.Row="2"
                  x:Name="PaneCombo"
                  DisplayMemberPath="Label"
                  SelectedIndex="0"/>

        <!-- Text label -->
        <TextBlock Grid.Row="4"
                   Text="전송할 텍스트"
                   Foreground="{StaticResource MutedBrush}"
                   FontFamily="Cascadia Code, Consolas"
                   FontSize="11"/>

        <!-- Text input (multiline) -->
        <TextBox Grid.Row="6"
                 x:Name="TextInputBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 MinLines="4"
                 MaxLines="8"
                 VerticalScrollBarVisibility="Auto"/>

        <!-- Newline option -->
        <CheckBox Grid.Row="8"
                  x:Name="AppendNewlineCheck"
                  Content="줄바꿈 자동 추가 (↵ Enter)  — 명령 실행에 필요"
                  IsChecked="True"
                  Margin="0,0,0,0"/>
    </Grid>

    <!-- Re-root the entire content as a single DockPanel so the footer bar
         sits outside the scrollable main content area. -->
    <Window.Template>
        <ControlTemplate TargetType="Window">
            <Border Background="{StaticResource BgBrush}">
                <DockPanel>
                    <!-- Footer -->
                    <Border DockPanel.Dock="Bottom"
                            Background="{StaticResource ChromeBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="0,1,0,0"
                            Padding="20,10">
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <Button x:Name="CancelBtn"
                                    Style="{StaticResource SecondaryButton}"
                                    Content="취소"
                                    IsCancel="True"
                                    Margin="0,0,8,0"
                                    Click="OnCancel"/>
                            <Button x:Name="SendBtn"
                                    Style="{StaticResource PrimaryButton}"
                                    Content="전송  ↵"
                                    IsDefault="True"
                                    Click="OnSend"/>
                        </StackPanel>
                    </Border>

                    <!-- Main content area -->
                    <ContentPresenter/>
                </DockPanel>
            </Border>
        </ControlTemplate>
    </Window.Template>
</Window>
